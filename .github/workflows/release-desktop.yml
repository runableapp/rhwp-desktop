name: Build Desktop Releases

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"
      - "desktop-v*"

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install Rust toolchain (rhwp WASM)
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Install wasm-pack
        run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh

      - name: Sync latest rhwp (WASM + UI)
        working-directory: ${{ github.workspace }}
        env:
          GIT_LFS_SKIP_SMUDGE: "1"
        run: |
          chmod +x "${GITHUB_WORKSPACE}/scripts/build-rhwp-wasm-and-sync.sh"
          RHWP_BUILD_METHOD=wasm-pack "${GITHUB_WORKSPACE}/scripts/build-rhwp-wasm-and-sync.sh"

      - name: Install Linux build dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y binutils fakeroot rpm squashfs-tools

      - name: Install desktop deps
        working-directory: desktop
        run: npm ci

      - name: Install UI deps
        working-directory: ui
        run: npm install

      - name: Build UI
        working-directory: ui
        run: npm run build

      - name: Prepare icons (base png)
        working-directory: desktop
        run: npm run prepare:icons

      - name: Prepare icons (Windows .ico)
        if: runner.os == 'Windows'
        working-directory: desktop
        run: node scripts/gen-win-ico.mjs

      - name: Prepare icons (macOS .icns)
        if: runner.os == 'macOS'
        working-directory: desktop
        run: ./scripts/gen-mac-icns.sh

      - name: Build desktop
        working-directory: desktop
        run: npm run build

      - name: Package (Linux AppImage)
        if: runner.os == 'Linux'
        working-directory: desktop
        run: npx electron-builder --linux AppImage --publish never

      - name: Package (Windows)
        if: runner.os == 'Windows'
        working-directory: desktop
        run: npx electron-builder --win nsis --publish never

      - name: Package (macOS)
        if: runner.os == 'macOS'
        working-directory: desktop
        run: npx electron-builder --mac dmg --publish never

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: desktop-${{ runner.os }}
          path: |
            desktop/release/*.AppImage
            desktop/release/*.exe
            desktop/release/*.dmg
            desktop/release/*.zip
            desktop/release/latest*.yml

  create-release:
    name: Create GitHub Release (attach binaries)
    if: startsWith(github.ref, 'refs/tags/')
    needs: [build]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-artifacts
          pattern: "desktop-*"
          merge-multiple: true

      - name: List release files
        shell: bash
        run: |
          set -euo pipefail
          ls -la release-artifacts || true
          echo "==> Found files:"
          find release-artifacts -type f -maxdepth 2 -print

      - name: Create/Update GitHub Release and upload assets
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: Desktop Release ${{ github.ref_name }}
          draft: false
          prerelease: false
          files: |
            release-artifacts/**/*.AppImage
            release-artifacts/**/*.exe
            release-artifacts/**/*.dmg
            release-artifacts/**/*.zip
            release-artifacts/**/latest*.yml

